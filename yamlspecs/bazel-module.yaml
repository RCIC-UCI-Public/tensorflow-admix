# Builds the module for bazel.
!include bazel.yaml
!include rcic-module.yaml
---
- package: bazel module 
  category: TOOLS
  release: 2
  module:
    prepend_path:
      - PATH {{root}}/bin
    prereq:
